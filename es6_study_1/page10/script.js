// Declare the language variable
const language="French";

// Output the value of the language variable
console.log(language);

// Use the language variable to output "I can speak ____"
console.log("I can speak "+language);
